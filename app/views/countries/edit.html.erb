<div class='button'><%= link_to 'All', countries_path %>
<h3>Editing country</h3>

<% form_for(@country) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update',:class=>'button_new' %>
  </p>
<% end %>

