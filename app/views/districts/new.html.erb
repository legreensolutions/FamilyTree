<div class='button'><%= link_to 'All', districts_path %>
<h3>New district</h3>

<%= form_for(@district) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %>:
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

