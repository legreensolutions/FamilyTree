<% if current_user and @user.id == current_user.id %>

<% end %><br />
<div align="center">
<h3>Edit Account</h3>

<% form_for @user do |f| %>
  <%= f.error_messages %>
  <%= render :partial => "form", :object => f %>
  <%= f.submit "Update",:class=>'button_new' %>
<% end %>
</div>
<br />
  <div style="text-align:center;">
    <%= link_to 'Click here to change password',:controller=>'password_resets',:action=>'change_password' %>
  </div>
  <div class="button" style="text-align:center;">
    <%= link_to 'Change',:controller=>'password_resets',:action=>'change_password' %>
  </div>

